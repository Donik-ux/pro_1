{
  "name": "pro-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && cd ../backend && npm install --legacy-peer-deps",
    "start": "node backend/server.js",
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "postinstall": "npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps"
  }
}
